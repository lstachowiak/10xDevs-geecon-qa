---
import Layout from "@/layouts/Layout.astro";
import SessionsList from "@/components/moderator/SessionsList";

// Disable prerendering for this page to enable server-side checks
export const prerender = false;

// Authentication is handled by middleware
// User will be redirected to "/" if not authenticated
---

<Layout title="Sessions - Moderator Panel">
  <SessionsList client:load />
</Layout>
